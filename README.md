# Portfolio site

will try my best to keep this up to date.
built with react + vite.

## Cloudflare Pages

Build command: `npm run build`

Build output directory: `dist`

For CLI deploys, run:

```sh
npm run deploy:cloudflare
```
